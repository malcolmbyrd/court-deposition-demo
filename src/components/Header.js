const Header = () => (
  <header>
    <h1>Available Court Deposition Jobs Needing a Provider</h1>
    <div>
      Please click on the jobs button to see all available jobs without a provider.
    </div>
  </header>
)

export default Header
